apply plugin: 'com.android.application'
apply from: 'tinkerpatch.gradle'

android {
    compileSdkVersion 26
    buildToolsVersion "26.0.2"
    defaultConfig {
        applicationId "com.hhz.hotfix"
        minSdkVersion 16
        targetSdkVersion 26
        versionCode 2
        versionName "1.0.2"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }

    //需要配置自己的签名
    signingConfigs {
        release {
            keyAlias 'key0'
            keyPassword 'hhh-0607'
            storeFile file('C:/Users/hzhao/Desktop/hbkey.jks')
            storePassword 'hhh-0607'
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:26.1.0'
    // 若使用annotation需要单独引用,对于tinker的其他库都无需再引用
    provided 'com.tencent.tinker:tinker-android-anno:1.9.2'
    compile 'com.tinkerpatch.sdk:tinkerpatch-android-sdk:1.2.2'
}
